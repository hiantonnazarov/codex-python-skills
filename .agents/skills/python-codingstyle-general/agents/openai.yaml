interface:
  display_name: "Python Coding Style"
  short_description: "Default Python coding rules for Codex"
  default_prompt: "Use $python-codingstyle-general whenever writing or refactoring Python implementation code so it stays small, typed, and maintainable. Keep tests separate, split modules by responsibility and contract boundaries, and avoid generic utils or helpers dumping grounds."
